# dependencies
/node_modules

# next.js build output
/.next/
/out/

# generated by scripts/optimize-media.mjs -- regenerated on every build
/public/_gen/
/src/lib/media-manifest.json

# ---------------------------------------------------------------------------
# Private working files. The public repo is Lenar's, and he is not a developer:
# README.md is the ONLY documentation he should ever see. Everything below stays
# on Clint's machine -- still there locally, just never pushed.
# ---------------------------------------------------------------------------
/_INBOX/
/BUILD-SPEC.md
/CLINT-RUNBOOK.md

# Auto-written by `next dev` on every run. Pure AI-agent scaffolding, nothing to
# do with the site -- Next.js keeps recreating them, so they stay ignored.
/AGENTS.md
/CLAUDE.md

# env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
next-env.d.ts
tsconfig.tsbuildinfo
